
import numpy as np

#this command imports a python package called 'numpy', which allows us to use extra commands.
#since I said import 'as np', that means I use np to invoke it.
#if this causes an error, you might not have the numpy package installed.

if np.sqrt(65) > 8:
 print("Yes!")
